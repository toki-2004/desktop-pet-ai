# desktop-pet 交接文档（2026-08-27）

## 状态

- 桌宠核心已完成首版，冒烟测试与组件测试通过。
- 2026-08-27 第二轮：新增滚轮缩放、左键摸头交互、自言自语文本库、
  余额样式与字号滑块、设置后自动重启（后改为热生效）。
- 2026-08-27 第三轮：设置改为**热生效（不再重启）**；账号系统升级为
  **多平台**（DeepSeek/Kimi/硅基流动），适配器架构可扩展。
- 2026-08-27 第四轮：新增**开机自启**（HKCU Run 键 + pythonw 静默启动，
  右键菜单开关）；准备推送 GitHub。
- 默认图片已复制到 `assets/deepseek拟人.png`（源文件
  `D:\pythonitems\deepseek拟人.png`）；摸头动图复制到
  `assets/ds摸头.gif`（源文件 `D:\pythonitems\ds摸头.gif`）。

## 模块结构

| 文件 | 职责 |
| --- | --- |
| `main.py` | 入口：组装各模块、信号接线、位置记忆、冒烟模式（`PET_SMOKE=1` 自动退出） |
| `config.py` | 配置读写（`config.json`，默认值见 `DEFAULT_CONFIG`） |
| `pet_window.py` | 透明置顶主窗口：显示宠物（PNG/GIF）、滚轮缩放、拖动、左键交互（单击一次/长按循环）、右键菜单、余额文本、浮动动画 |
| `balloon.py` | 思考云线气泡：可自定义颜色、文字居中、persistent 模式停留到确认 |
| `balance.py` | DeepSeek 余额轮询（后台线程），增减发信号 |
| `scheduler.py` | 高峰/空闲时段判断（北京时间）与切换信号 |
| `settings_dialog.py` | 设置对话框：通知文本/颜色（Qt 取色器含吸管+十六进制）、自言自语文本库与间隔、外观导入、交互图片、余额字号滑块、账号绑定 |
| `main.py` | 入口：组装各模块、信号接线、位置记忆、自言自语监控、设置后自动重启、冒烟模式（`PET_SMOKE=1`） |
| `platforms.py` | 多平台余额适配器：`Provider` 注册表 + `fetch(api_key) -> (余额, 币种)` |
| `autostart.py` | 开机自启：读写 HKCU Run 键（当前用户级，无需管理员） |

## 测试情况

- `python main.py`（PET_SMOKE=1）：启动、弹通知、浮动动画、退出，无异常。
- `ThinkingBalloon`、`SettingsDialog`、`AccountDialog` 构造正常。
- 时段逻辑 10 个边界用例全部通过（9:00/11:59/12:00/13:59/14:00/18:00/周末等）。
- 滚轮缩放、摸头交互（显示/回退）、余额字号应用：程序化验证通过。
- 旧账号格式自动迁移、DeepSeek 真实余额查询：已验证。
- 已知未实测：Kimi / 硅基流动接口（需对应平台有效 API Key 验证）；
  余额按人民币合计显示，接入美元平台时需扩展币种聚合。

## 已知限制与下一步

- 余额显示与浮动动画样式较基础，可进一步美化（毛玻璃、粒子等）。
- 多显示器下气泡位置按锚点所在屏幕简单钳制，未做精细处理。
- 未做托盘图标，退出需用右键菜单。
- 设置修改已热生效（各模块按需重读配置）；若某次改动未生效，检查对应模块
  是否在 `_on_settings_applied` / `apply_config` 中接入了重载。
- 下一步可选：PyInstaller 打包 exe 并发布 Release（按项目惯例 vX.Y.Z）；
  通知增加图标/音效；自言自语加入时间感知（问候语/夜间文案）。

## 安全提醒

`config.json` 含明文 API Key，已加入 `.gitignore`，任何情况下不要提交。
